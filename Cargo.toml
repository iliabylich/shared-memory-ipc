[package]
edition = "2021"
name = "native-ipc-rust"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"

[dev-dependencies]
jemallocator = "0.5.0"
rand = "0.8"
